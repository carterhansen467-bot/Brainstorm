# macOS
.DS_Store

# Editor / working backups
*.bak
*.bak2

# Local debug & diagnostics output
brainstorm_diagnostics.txt

# User state, rewritten by the mod at runtime (never track: tracked copies
# made every git pull conflict for players)
settings.lua
debug_predict.txt
